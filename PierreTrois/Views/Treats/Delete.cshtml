@{
  Layout = "_Layout";
}

@model PierreTrois.Models.Treat

<p>Are you sure you want to delete this treat entry from the database?</p>

<p><strong>@Html.DisplayNameFor(targetTreat => targetTreat.Name)</strong>: @Html.DisplayFor(targetTreat => targetTreat.Name)</p>

@using(Html.BeginForm())
{
  <input type="submit" value="Delete Treat" class="btn btn-danger" />
}

<hr/>
@Html.ActionLink("Back to Treat Details", "Details", new { id = Model.TreatId })