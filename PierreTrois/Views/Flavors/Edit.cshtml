@{
  Layout = "_Layout";
}

@model PierreTrois.Models.Flavor

<p>Edit the information below and click Save to submit changes.</p>
<hr/>
@using(Html.BeginForm())
{
  <p>@Html.DisplayNameFor(entry => entry.Name): @Html.EditorFor(entry => entry.Name) <input type="submit" value="Save Edits"/></p>
}
<hr/>
<p>@Html.ActionLink("Back to Flavor details", "Details", new { id = Model.FlavorId })</p>