@{
  Layout = "_Layout";
}

@using PierreTrois.Models;

@if (@Model.Count == 0)
{
  <p>There are no Flavors to display</p>
}

@foreach (Flavor flavor in Model)
{
  <li>@Html.ActionLink($"{flavor.Name}", "Details", new { id = flavor.FlavorId })</li>
}
<hr/>
<p>@Html.ActionLink("Create Flavor", "Create")</p>